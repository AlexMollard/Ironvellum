# Public cloud endpoints, committed so F-Droid's build server (which has no
# local.properties) can build the app. SAFE TO COMMIT BY DESIGN: this is the
# public project URL and the *publishable* (anon) key — the same values are
# already inside every shipped APK, and row-level security is what protects
# the data, not key secrecy. Forks: replace both with your own Supabase
# project's values (or leave them to sync to the shared project).
# local.properties overrides either key without editing this file.
supabase.url=https://nhrresfzfhpxagnqrbzm.supabase.co
supabase.key=sb_publishable_vVObF6OFLFtTU0TQI8GASw_3h7lX-Cz
