package com.ironvellum.app.domain

/**
 * Daily step target. Shared by the dashboard readout and the stats charts so
 * the two can never disagree about what "hit the goal" means.
 */
const val STEP_GOAL = 10_000
