<?xml version="1.0" encoding="utf-8"?>
<!--
  Only the export staging directory is shared. The database, its snapshots and
  the crash journal live elsewhere in private storage and stay unreachable
  through this provider.
-->
<paths>
    <cache-path name="exports" path="exports/" />
</paths>
